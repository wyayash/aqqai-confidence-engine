# AQQAI Requirements
# Install with: pip install -r requirements.txt

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.0
scikit-learn==1.5.2
numpy==1.26.4
requests==2.31.0
loguru==0.7.2

# Coherence scoring — all-MiniLM-L6-v2 is pre-baked into the Docker image
# (downloaded at build time, not runtime)
sentence-transformers==3.1.1